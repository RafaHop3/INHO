# network.tf — REMOVED (Serverless Edition)
#
# AWS Lambda runs on AWS-managed infrastructure. There is no VPC, no subnets,
# no NAT Gateway, and no Internet Gateway to provision or pay for.
# This file is intentionally left as a stub so git history is preserved.
#
# If you ever need VPC access (e.g., to connect Lambda to a private RDS),
# re-introduce the VPC config here and add vpc_config to aws_lambda_function.
